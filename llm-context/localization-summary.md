# Localization Summary

## Status
- **Libraries**: `i18next` and `react-i18next` are installed dependencies.
- **Implementation**: Not yet configured in the codebase.
- **Current Language**: Polish (hardcoded in UI components).

## Plan (Phase 3)
- Initialize `i18n` instance in `src/i18n.ts` (to be created).
- Extract hardcoded strings to translation files (e.g., `public/locales/pl/translation.json`).
- Implement language switcher in Navbar.

