# Environment Variables Template
# Copy this content to create your .env files

# =============================================================================
# ROOT .env (for Docker Compose)
# =============================================================================

POSTGRES_DB=antystics
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_20_CHARS

ASPNETCORE_ENVIRONMENT=Production
JWT_SECRET=CHANGE_ME_RANDOM_64_CHARACTER_STRING_USE_OPENSSL_RAND_BASE64_48
JWT_ISSUER=Antystics
JWT_AUDIENCE=AntysticsUsers

# Social login secrets
GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-gmail-app-password
EMAIL_FROM_ADDRESS=noreply@antystyki.pl
EMAIL_FROM_NAME=Antystyki

VITE_API_URL=http://localhost:5000/api
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com

# For production, change to:
# VITE_API_URL=https://api.antystyki.pl/api

# =============================================================================
# frontend/.env (for Frontend)
# =============================================================================

VITE_API_URL=http://localhost:5000/api
VITE_RECAPTCHA_SITE_KEY=your-recaptcha-site-key-from-google

# =============================================================================
# backend/.env (for Backend - if needed)
# =============================================================================

DB_HOST=postgres
DB_PORT=5432
DB_NAME=antystics
DB_USER=postgres
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD

JWT_SECRET=CHANGE_ME_RANDOM_64_CHARACTER_STRING
JWT_ISSUER=Antystics
JWT_AUDIENCE=AntysticsUsers

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_FROM_ADDRESS=noreply@antystyki.pl
EMAIL_FROM_NAME=Antystyki

STORAGE_LOCAL_PATH=wwwroot/uploads
ASPNETCORE_ENVIRONMENT=Production

RECAPTCHA_SECRET_KEY=your-recaptcha-secret-key-from-google
RECAPTCHA_SITE_KEY=your-recaptcha-site-key-from-google

# =============================================================================
# How to Generate Secure Secrets
# =============================================================================

# Database Password (32 characters):
#   openssl rand -base64 24

# JWT Secret (64 characters):
#   openssl rand -base64 48

# Gmail App Password:
#   1. Enable 2FA on Gmail
#   2. Go to https://myaccount.google.com/apppasswords
#   3. Generate app password for "Mail"

# reCAPTCHA Keys:
#   1. Go to https://www.google.com/recaptcha/admin/create
#   2. Register your site with reCAPTCHA v3
#   3. Copy Site Key (public) and Secret Key (private)

# =============================================================================
# Quick Setup Commands
# =============================================================================

# Create root .env:
# cat ENV_TEMPLATE.txt | sed -n '/^# ROOT/,/^$/p' > .env

# Create frontend/.env:
# cat ENV_TEMPLATE.txt | sed -n '/^# frontend/,/^$/p' > frontend/.env

# Or manually copy sections above to respective .env files

